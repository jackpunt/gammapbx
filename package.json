{
  "name": "gammapbx",
  "version": "1.0.1",
  "description": "protobuf for gamma display client; typescript, javascript and java",
  "main": "./GammaProto.ts",
  "files": [
    "gamma/player/util/GammaProto.java",
    "src/*.proto",
    "*.js",
    "*.ts"
  ],
  "typings": "GammaProto.d.ts",
  "scripts": {
    "java": "protoc -I src --java_out=. src/*.proto",
    "dts": "tsc GammaProto.ts --target es2015 --declaration --emitDeclarationOnly --allowJs",
    "protots": "protoc -I src --ts_out=. src/*.proto",
    "protojs": "protoc -I src --js_out=library=GammaProto,binary:. src/*.proto",
    "clean": "rm -f *.js *.ts src/*.js",
    "build": "bash -c 'npm run clean; npm run protojs; npm run protots; npm run dts'",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/jackpunt/gammapbx.git"
  },
  "keywords": [
    "gamma",
    "typescript",
    "client",
    "protobuf"
  ],
  "author": "Jeff Peck <jpeckj@gmail.com> (http://thegraid.com)",
  "license": "MIT",
  "bugs": {
    "url": "https://github.com/jackpunt/gammapbx/issues"
  },
  "homepage": "https://github.com/jackpunt/gammapbx#readme",
  "dependencies": {
    "@types/google-protobuf": "^3.7.4",
    "google-protobuf": "^3.2.0",
    "protoc-gen-ts": "^0.3.5"
  },
  "devDependencies": {
    "typescript": "^4.2.3"
  }
}
